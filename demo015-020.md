# Java 后端循序渐进教学项目（demo016 - demo020）

## 系列说明

demo001-015 完成了从最原始 JDBC 到 Spring Boot + MyBatis-Plus + 统一返回 + 全局异常 + 事务 + 校验 + REST API 的完整演进教学。

demo016-020 阶段，我们**不再大幅增加业务功能**，而是在原有 Student 系统基础上，系统性地补齐**生产级基础设施能力**，并进行一次完整项目重构，为后续真正做「小型电商系统」打下坚实基础。

---

## 版本演进总览（demo016-020）

| Demo   | 主题                              | 核心变化                              | 教学价值 |
|--------|-----------------------------------|---------------------------------------|----------|
| 016    | 统一日志 + 请求响应日志           | MDC + LoggingFilter + 完整 Request/Response Body 记录 + 敏感信息脱敏 | 生产级日志规范 |
| 017    | Redis 缓存集成                    | @Cacheable + 手动 RedisTemplate + 缓存穿透防护（空值缓存 + 随机 TTL） | 缓存最佳实践 |
| 018    | JWT + Spring Security 基础认证    | 无状态 JWT 登录 + 自定义过滤器 + 基于 sys_user 表的真实认证 | 现代认证体系 |
| 019    | Docker + Docker Compose 一键部署  | 多阶段 Dockerfile + 完整编排（App + MySQL + Redis）+ 健康检查 | 容器化部署能力 |
| 020    | 完整项目重构                      | DTO/VO 彻底分离 + 目录模块化 + 代码规范 + 转换逻辑收敛 | 真实项目架构意识 |

---

## 详细版本说明

### demo016 - 统一日志 + 请求响应日志（MDC + 拦截器）

- 引入 `LoggingFilter`（基于 `OncePerRequestFilter` + `ContentCachingRequestWrapper`）
- 实现**完整请求体 + 响应体**记录
- MDC 传递 traceId，全链路日志可串联
- 敏感字段自动脱敏（password、token）
- 日志格式专业化（logback-spring.xml）

**里程碑意义**：让系统具备了生产环境最基础的「可观测性」。

### demo017 - Redis 缓存集成（@Cacheable + 缓存穿透处理）

- 集成 `spring-boot-starter-data-redis`
- 自定义 `RedisTemplate`（Jackson 序列化）
- 使用 `@Cacheable` + `@CacheEvict`
- 手动实现**缓存穿透防护**：空值缓存 + 随机过期时间（防雪崩）
- 新增 `/students/protect/{id}` 专门用于演示防护效果

**里程碑意义**：掌握缓存这把「双刃剑」的正确用法。

### demo018 - JWT + Spring Security 基础认证

- 引入 Spring Security 6 + `java-jwt`
- 实现 `/auth/login` 签发 JWT
- `JwtAuthenticationFilter` 拦截所有请求并注入 SecurityContext
- 基于 `sys_user` 表做真实用户认证
- 角色支持（ADMIN / TEACHER / STUDENT）
- 自定义 `UserDetailsService` 消除默认密码警告

**里程碑意义**：从「谁都能调」进化到「必须带合法 Token」。

### demo019 - Docker + Docker Compose 一键部署

- 多阶段 `Dockerfile`（构建镜像极小 + 非 root 运行）
- 完整 `docker-compose.yml`（app + mysql + redis）
- 环境变量覆盖配置（Docker 内使用服务名）
- 健康检查 + readiness probe
- `.dockerignore` 优化构建

**里程碑意义**：项目具备了「开箱即用」的部署能力。

### demo020 - 完整项目重构（模块划分 + 代码规范）

- **DTO / VO 彻底分离**：Controller 只返回 VO，不再暴露 Entity
- 新增 `StudentVO`，Controller 增加转换逻辑（演示 MapStruct 雏形）
- 强化 `common/` 公共模块
- 为后续按 `module/` 领域拆分做好架构准备
- 整体目录结构和命名更加规范

**里程碑意义**：从「能跑」进化到「好维护、好扩展」。

---

## 当前完成状态

- **demo001 ~ demo020** 全部完成
- 基础设施能力完整（日志、缓存、认证、部署、结构）
- 代码质量和架构意识显著提升
- 所有 demo 均支持本地 + Docker 两种运行方式

---

## 下一步规划：真正的小型电商系统（Mini E-commerce）

经过 demo016-020 的基础设施锤炼，我们终于可以开始做**真正有业务价值的项目**了。

### 项目定位

**小型电商系统（Mini Mall）**，而非简单的 Todo / 学生系统。

核心模块（第一阶段 MVP）：
- 用户体系（注册、登录、JWT、地址管理）
- 商品体系（分类、SPU、SKU、库存）
- 购物车
- 订单系统（下单事务、库存扣减、订单状态流转）
- 简单支付模拟

### 技术栈（最终版）

**后端**（基于 demo020 架构继续演进）：
- Spring Boot 3 + MyBatis-Plus
- Redis（缓存 + 分布式锁）
- JWT + Spring Security
- Docker + Docker Compose
- 后续可加入：RabbitMQ / RocketMQ、Seata、Sentinel 等

**前端**（全新引入）：
- Vite + Vue 3 + TypeScript
- Pinia + Vue Router
- Element Plus（或 Ant Design Vue）
- Axios + 统一拦截器
- 后期考虑：后管系统 + C 端 H5

### 推荐演进路线（demo021_mall 之后）

| 版本            | 主题                              | 核心交付                                      |
|-----------------|-----------------------------------|-----------------------------------------------|
| demo021_mall    | Phase 1-3（用户+商品+购物车）     | JWT 认证、Category/SPU/SKU、购物车            |
| demo022_mall    | 订单系统                          | 下单事务、库存扣减、订单状态机                |
| demo023_mall    | 支付模拟 + 完整交易闭环           | 支付回调、订单取消、退款流程                  |
| demo024_mall    | Redis 实战                        | 缓存策略、分布式锁、库存防超卖                |
| 后续版本        | 工程化 + 前后端分离               | 后管系统（Vue3）、部署、监控、CI/CD           |

### 目录结构建议（新项目 - 已按实际落地调整）

```
demo021_mall/          # 第一个真实业务版本（Phase 1-3 已完成）
├── src/main/java/com/mall/
│   ├── common/
│   ├── module/
│   │   ├── auth/
│   │   ├── user/
│   │   ├── product/category/
│   │   ├── product/spu/
│   │   ├── product/sku/
│   │   └── cart/
│   └── MallApplication.java
├── sql/init.sql
├── docker-compose.yml
└── Phase1-3.md        # 阶段演进详细文档
```

---

**说明**

demo001-015 是「看清底层」的教学阶段。  
demo016-020 是「补齐生产能力 + 重构」的准备阶段。  
**demo021_mall 开始**，我们正式进入「做真实业务系统」的阶段（并会一直演进到 demo030+）。

从现在开始，每一个功能都要考虑：
- 缓存策略
- 事务边界
- 权限控制
- 接口设计（DTO/VO）
- 部署友好性

**实际落地情况**：demo021_mall 已完成用户认证 + 商品三级模型 + 购物车（详见 demo021_mall/Phase1-3.md）。

准备好继续演进了吗？下一步最推荐的是 **demo022_mall - 订单系统（事务 + 库存扣减）**。

直接告诉我下一步怎么走。